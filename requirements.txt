PyGObject>=3.42        # GTK3 bindings (same UI toolkit as KlipperScreen)
pyserial>=3.5           # GRBL serial communication
opencv-python-headless>=4.8   # registration mark detection
numpy>=1.24
Flask>=2.3              # WiFi/Ethernet gcode upload server
Pillow>=10.0             # frame -> GdkPixbuf conversion helpers

# System packages needed alongside pip (Raspberry Pi OS / Debian):
#   sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-3.0 \
#                     python3-opencv libatlas-base-dev
